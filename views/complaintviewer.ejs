<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <title>Document</title>
</head>
<body class = "container">
    <div>
        <h3 class="fs-2"><%= data.email %></h3>
        <% if(data.remembered) { %> 
            <a href="/admin/complaint/<%=data.id%>/toforget" class = "btn btn-primary">forget</a>
        <% } else { %>
            <a href="/admin/complaint/<%=data.id%>/toremember" class = "btn btn-primary">remember</a>
        <% } %>
        <% if(data.answered) { %> 
        <a href="/admin/complaint/<%=data.id%>/delete" class="btn btn-danger">delete the complaint</a> 
        <% } %> 
        <p class=""><span class = "fs-3">Complaint :</span><br>
        <span class=""><%= data.complaintstatement %></span> </p>
        <% if(data.usersolution) { %> 
        <p><span class="fs-3">Student's solution :</span><br>
        <span><%= data.userview %></span></p>
        <% } %> 
    </div>
    <div>
        <form action="/admin/complaint/<%=data.id%>/solution" method = "POST">
            <label for="adminsolution">write the solution here : </label><br>
            <textarea name="adminsolution" id="adminsolution" style = "width : 400px ; height : 150px ;"></textarea><br>
            <button class = "btn btn-success">Done</button>
        </form>
    </div>
</body>
</html>